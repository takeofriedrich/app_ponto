import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:monitoria_app/app/modules/start/submodulos/alunos/alunos_page.dart';

void main() {
  testWidgets('AlunosPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(AlunosPage(title: 'Alunos')));
    //  final titleFinder = find.text('Alunos');
    //  expect(titleFinder, findsOneWidget);
  });
}
