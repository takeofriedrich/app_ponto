import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:monitoria_app/app/modules/start/submodulos/atendimentos/atendimentos_controller.dart';
import 'package:monitoria_app/app/modules/start/submodulos/atendimentos/atendimentos_module.dart';

void main() {
  initModule(AtendimentosModule());
  // AtendimentosController atendimentos;
  //
  setUp(() {
    //     atendimentos = AtendimentosModule.to.get<AtendimentosController>();
  });

  group('AtendimentosController Test', () {
    //   test("First Test", () {
    //     expect(atendimentos, isInstanceOf<AtendimentosController>());
    //   });

    //   test("Set Value", () {
    //     expect(atendimentos.value, equals(0));
    //     atendimentos.increment();
    //     expect(atendimentos.value, equals(1));
    //   });
  });
}
