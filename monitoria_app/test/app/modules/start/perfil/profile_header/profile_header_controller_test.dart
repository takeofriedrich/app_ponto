import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:monitoria_app/app/modules/start/perfil/profile_header/profile_header_controller.dart';
import 'package:monitoria_app/app/modules/start/start_module.dart';

void main() {
  initModule(StartModule());
  // ProfileHeaderController profileheader;
  //
  setUp(() {
    //     profileheader = StartModule.to.get<ProfileHeaderController>();
  });

  group('ProfileHeaderController Test', () {
    //   test("First Test", () {
    //     expect(profileheader, isInstanceOf<ProfileHeaderController>());
    //   });

    //   test("Set Value", () {
    //     expect(profileheader.value, equals(0));
    //     profileheader.increment();
    //     expect(profileheader.value, equals(1));
    //   });
  });
}
