import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:monitoria_app/app/modules/start/perfil_page/perfil_page_controller.dart';
import 'package:monitoria_app/app/modules/start/start_module.dart';

void main() {
  initModule(StartModule());
  // PerfilPageController perfilpage;
  //
  setUp(() {
    //     perfilpage = StartModule.to.get<PerfilPageController>();
  });

  group('PerfilPageController Test', () {
    //   test("First Test", () {
    //     expect(perfilpage, isInstanceOf<PerfilPageController>());
    //   });

    //   test("Set Value", () {
    //     expect(perfilpage.value, equals(0));
    //     perfilpage.increment();
    //     expect(perfilpage.value, equals(1));
    //   });
  });
}
